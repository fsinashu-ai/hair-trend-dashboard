# AGENTS.md

このアプリで Codex や他の開発エージェントが作業するときのルールです。

## 基本方針

- 初心者が読みやすく、編集しやすいコードを優先する。
- 難しい抽象化を急がず、画面・部品・データ・型の置き場所を分かりやすく分ける。
- 変数名、関数名、コンポーネント名は役割が伝わる名前にする。
- コメントは必要な場所だけに短く書く。

## 禁止事項

- SNSスクレイピングは禁止。
- Instagram、TikTok、Xなどから投稿や本文を自動取得しない。
- 著作権のある本文やSNS投稿内容を無断保存しない。
- APIキーや秘密情報をコードに直接書かない。

## 環境変数

- 環境変数は `.env.local` に置く。
- `.env.local` は Git に含めない。
- ブラウザに公開してよい値だけ `NEXT_PUBLIC_` を付ける。
- Gemini APIキーやSupabaseの秘密鍵はサーバー側だけで使う。

## UIルール

- UIはシンプルで、美容サロン向けの落ち着いた見た目にする。
- 業務で使いやすいように、情報を探しやすく、読みやすく配置する。
- 色や装飾を増やしすぎず、余白・文字サイズ・コントラストを丁寧に整える。
- 最初のMVPでは、派手な演出よりも入力・一覧・確認の分かりやすさを優先する。

## 開発ルール

- Next.js App Router、TypeScript、Tailwind CSS の既存構成に合わせる。
- ページは `src/app`、共通部品は `src/components`、ダミーデータは `src/data`、型は `src/types` に置く。
- まずはダミーデータで画面を作り、あとからSupabaseやGemini APIに接続する。
- 変更後は必ず `npm run lint` で確認する。

PowerShellで `npm` が実行ポリシーにより止まる場合は、同じ確認として `npm.cmd run lint` を使ってよい。

## GitHubアップロードルール

- アップロード前に、変更したファイル一覧を整理する。
- 既存ファイルはGitHub上のSHAを確認して更新し、新規ファイルは新規作成としてアップロードする。
- `.env.local`、APIキー、パスワード、個人情報を含むファイルはアップロードしない。
- `public` 配下に `.env.local` やAPIキーを含むファイルを置かない。`public` 配下は公開対象になる。
- `supabase/schema.sql` を変更した場合は、READMEにもSupabase側で再実行が必要なことを書く。
- アップロード後は、VercelのDeploymentsでビルド結果を確認する。
- 失敗した場合は、エラーログのファイル名と行番号を見て、該当ファイルだけを修正して再アップロードする。
